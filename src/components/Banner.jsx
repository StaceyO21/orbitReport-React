export default function Banner() {
    return (
        <div>
         <header id="Banner">Orbit Report</header>
        <head>
            <link rel = "stylesheet" type = "text/css" href = "styles.css" />
            </head>
        <p>
            Click on the buttons to see the satellites in that orbit type
        </p>
        </div>
    );
}